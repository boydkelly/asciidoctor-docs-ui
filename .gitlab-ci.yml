image: node:10.14.2-stretch
stages: [deploy]
bundle-stable:
  stage: deploy
  only:
  - julakan-main@boydkelly/julakan-docs-ui
  script:
  - npm install --quiet --no-progress
  - SOURCEMAPS=true node_modules/.bin/gulp bundle
  artifacts:
    paths:
    - build/ui-bundle.zip
trigger_build:
  stage: deploy
  script:
    - "curl -X POST -F token=62e9b9b81d2efb0b9ef285be686769 -F ref=main https://gitlab.com/api/v4/projects/19007974/trigger/pipeline"
